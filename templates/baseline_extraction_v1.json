{
  "id": "baseline_extraction_v1",
  "name": "Baseline Extraction",
  "description": "Baseline prompt for JSON extraction without additional patterns.",
  "version": "1.0.0",
  "placeholders": {
    "input": "string",
    "schema": "string",
    "constraints": "string"
  },
  "system_prompt": "",
  "user_prompt": "Extract information from the input and return JSON that matches this schema:\n$schema\n\nConstraints:\n$constraints\n\nInput:\n$input",
  "temperature": 0.1
}
